import { PipeCapitalizePipe } from './pipe-capitalize.pipe';

describe('PipeCapitalizePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCapitalizePipe();
    expect(pipe).toBeTruthy();
  });
});
